<UserControl x:Class="HelloID.Vault.Management.Views.ReferenceData.LocationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:controls="clr-namespace:HelloID.Vault.Management.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Panel -->
        <controls:ReferenceDataHeader Grid.Row="0"
                                      Title="Locations"
                                      Count="{Binding TotalCount, StringFormat='{}{0} locations'}"
                                      SearchPlaceholder="Search locations..."
                                      SearchText="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      RefreshCommand="{Binding RefreshCommand}"
                                      AddCommand="{Binding AddItemCommand}"
                                      ResetCommand="{Binding ResetSettingsCommand}"/>

        <!-- DataGrid Content -->
        <Grid Grid.Row="1">
            <DataGrid ItemsSource="{Binding Locations}"
                     SelectedItem="{Binding SelectedLocation}"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     CanUserAddRows="False"
                     CanUserDeleteRows="False"
                     SelectionMode="Single"
                     GridLinesVisibility="Horizontal"
                     HeadersVisibility="Column"
                     RowHeaderWidth="0"
                     BorderThickness="0">

                <!-- Context Menu -->
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Copy" Command="ApplicationCommands.Copy">
                            <MenuItem.Icon>
                                <ui:FontIcon Glyph="&#xE8C8;"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <Separator/>
                        <MenuItem Header="Edit" Command="{Binding EditItemCommand}">
                            <MenuItem.Icon>
                                <ui:FontIcon Glyph="&#xE70F;"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <Separator/>
                        <MenuItem Header="Delete" Command="{Binding DeleteItemCommand}">
                            <MenuItem.Icon>
                                <ui:FontIcon Glyph="&#xE74D;"/>
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </DataGrid.ContextMenu>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="External ID" Binding="{Binding ExternalId}" Width="300"/>
                    <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="200"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="250"/>
                    <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="200">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment" Value="Left"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Loading Overlay -->
            <Border Background="#CC000000"
                   Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ui:ProgressRing IsActive="{Binding IsBusy}"
                                Width="60"
                                Height="60"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
