<UserControl x:Class="HelloID.Vault.Management.Views.ReferenceData.CustomFieldsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1400">

    <UserControl.Resources>
        <!-- Table Filter Button Styles - BasedOn shared FilterButtonBaseStyle -->
        <Style x:Key="TableFilterAllButtonStyle" TargetType="Button" BasedOn="{StaticResource FilterButtonBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ShowAll}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundAccentBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource SystemControlBackgroundAccentBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource SystemControlForegroundAltHighBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TableFilterPersonsButtonStyle" TargetType="Button" BasedOn="{StaticResource FilterButtonBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ShowPersons}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundAccentBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource SystemControlBackgroundAccentBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource SystemControlForegroundAltHighBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TableFilterContractsButtonStyle" TargetType="Button" BasedOn="{StaticResource FilterButtonBaseStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ShowContracts}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundAccentBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource SystemControlBackgroundAccentBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource SystemControlForegroundAltHighBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Panel -->
        <Border Grid.Row="0"
                Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
                Padding="16"
                BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Title Section -->
                <StackPanel Grid.Column="0" Margin="0,0,16,0">
                    <TextBlock Text="Custom Fields"
                              FontSize="20"
                              FontWeight="SemiBold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="{Binding PersonFieldsCount, StringFormat='{}{0} person'}"
                                  FontSize="14"
                                  FontWeight="SemiBold"
                                  Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
                        <TextBlock Text=" • " FontSize="14" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" Margin="4,0"/>
                        <TextBlock Text="{Binding ContractFieldsCount, StringFormat='{}{0} contract'}"
                                  FontSize="14"
                                  FontWeight="SemiBold"
                                  Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
                        <TextBlock Text=" • " FontSize="14" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" Margin="4,0"/>
                        <TextBlock Text="{Binding TotalCount, StringFormat='{}{0} total'}"
                                  FontSize="14"
                                  FontWeight="SemiBold"
                                  Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Search and Filter -->
                <Grid Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Table Filter Buttons -->
                    <StackPanel Grid.Column="0"
                               Orientation="Horizontal"
                               VerticalAlignment="Center"
                               Margin="0,0,12,0">
                        <Button Content="All"
                                Command="{Binding ShowAllFieldsCommand}"
                                Style="{StaticResource TableFilterAllButtonStyle}"/>
                        <Button Content="Persons"
                                Command="{Binding ShowPersonFieldsCommand}"
                                Style="{StaticResource TableFilterPersonsButtonStyle}"/>
                        <Button Content="Contracts"
                                Command="{Binding ShowContractFieldsCommand}"
                                Style="{StaticResource TableFilterContractsButtonStyle}"/>
                    </StackPanel>

                    <!-- Search Box -->
                    <ui:AutoSuggestBox Grid.Column="1"
                                       PlaceholderText="Search custom fields..."
                                       Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                       QueryIcon="Find"
                                       VerticalAlignment="Center"
                                       MinWidth="600"
                                       MaxWidth="900"
                                       FontSize="14"/>
                </Grid>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <ui:AppBarButton Icon="Refresh"
                                    Label="Refresh"
                                    Command="{Binding RefreshCommand}"/>
                    <ui:AppBarButton Icon="Add"
                                    Label="Add"
                                    Command="{Binding AddItemCommand}"/>
                    <ui:AppBarButton Icon="Undo"
                                    Label="Reset Settings"
                                    Command="{Binding ResetSettingsCommand}"
                                    ToolTip="Clear search and selection"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- DataGrid Content -->
        <Grid Grid.Row="1">
            <DataGrid ItemsSource="{Binding CustomFields}"
                     SelectedItem="{Binding SelectedCustomField}"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     CanUserAddRows="False"
                     CanUserDeleteRows="False"
                     SelectionMode="Single"
                     GridLinesVisibility="Horizontal"
                     HeadersVisibility="Column"
                     RowHeaderWidth="0"
                     BorderThickness="0">

                <!-- Context Menu -->
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Copy" Command="ApplicationCommands.Copy">
                            <MenuItem.Icon>
                                <ui:FontIcon Glyph="&#xE8C8;"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <Separator/>
                        <MenuItem Header="Edit" Command="{Binding EditItemCommand}">
                            <MenuItem.Icon>
                                <ui:FontIcon Glyph="&#xE70F;"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <Separator/>
                        <MenuItem Header="Delete" Command="{Binding DeleteItemCommand}">
                            <MenuItem.Icon>
                                <ui:FontIcon Glyph="&#xE74D;"/>
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </DataGrid.ContextMenu>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Table" Binding="{Binding TableName}" Width="120"/>
                    <DataGridTextColumn Header="Field Key" Binding="{Binding FieldKey}" Width="200"/>
                    <DataGridTextColumn Header="Display Name" Binding="{Binding DisplayName}" Width="250"/>
                    <DataGridTextColumn Header="Sort Order" Binding="{Binding SortOrder}" Width="100"/>
                    <DataGridTextColumn Header="Help Text" Binding="{Binding HelpText}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Loading Overlay -->
            <Border Background="#CC000000"
                   Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ui:ProgressRing IsActive="{Binding IsBusy}"
                                Width="60"
                                Height="60"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
