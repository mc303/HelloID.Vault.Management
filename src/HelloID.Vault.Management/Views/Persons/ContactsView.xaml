<UserControl x:Class="HelloID.Vault.Management.Views.Persons.ContactsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Panel -->
        <Border Grid.Row="0"
                Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
                Padding="16"
                BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Title Section -->
                <StackPanel Grid.Column="0" Margin="0,0,16,0">
                    <TextBlock Text="Contacts"
                              FontSize="20"
                              FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalCount, StringFormat='{}{0} contacts'}"
                              FontSize="14"
                              FontWeight="SemiBold"
                              Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                              Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Search Box -->
                <ui:AutoSuggestBox Grid.Column="1"
                                   PlaceholderText="Search contacts..."
                                   Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                   QueryIcon="Find"
                                   VerticalAlignment="Center"
                                   MaxWidth="800"
                                   HorizontalAlignment="Center" Width="800"/>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <ui:AppBarButton Icon="Refresh"
                                    Label="Refresh"
                                    Command="{Binding RefreshCommand}"/>
                    <ui:AppBarButton Icon="Add"
                                    Label="Add"
                                    Command="{Binding AddItemCommand}"/>
                    <ui:AppBarButton Icon="Undo"
                                    Label="Reset Settings"
                                    Command="{Binding ResetSettingsCommand}"
                                    ToolTip="Clear search and selection"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- DataGrid Content -->
        <Grid Grid.Row="1">
            <DataGrid ItemsSource="{Binding Contacts}"
                     SelectedItem="{Binding SelectedContact}"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     CanUserAddRows="False"
                     CanUserDeleteRows="False"
                     SelectionMode="Single"
                     GridLinesVisibility="Horizontal"
                     HeadersVisibility="Column"
                     RowHeaderWidth="0"
                     BorderThickness="0">

                <!-- Context Menu -->
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Copy" Command="ApplicationCommands.Copy">
                            <MenuItem.Icon>
                                <ui:FontIcon Glyph="&#xE8C8;"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <Separator/>
                        <MenuItem Header="Edit" Command="{Binding EditItemCommand}">
                            <MenuItem.Icon>
                                <ui:FontIcon Glyph="&#xE70F;"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <Separator/>
                        <MenuItem Header="Delete" Command="{Binding DeleteItemCommand}">
                            <MenuItem.Icon>
                                <ui:FontIcon Glyph="&#xE74D;"/>
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </DataGrid.ContextMenu>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Contact ID" Binding="{Binding ContactId}" Width="100"/>
                    <DataGridTextColumn Header="Person Name" Binding="{Binding PersonDisplayName}" Width="200"/>
                    <DataGridTextColumn Header="Person ID" Binding="{Binding PersonId}" Width="120"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                    <DataGridTextColumn Header="Phone Mobile" Binding="{Binding PhoneMobile}" Width="130"/>
                    <DataGridTextColumn Header="Phone Fixed" Binding="{Binding PhoneFixed}" Width="130"/>
                    <DataGridTextColumn Header="Address Street" Binding="{Binding AddressStreet}" Width="180"/>
                    <DataGridTextColumn Header="Street Ext" Binding="{Binding AddressStreetExt}" Width="100"/>
                    <DataGridTextColumn Header="House Number" Binding="{Binding AddressHouseNumber}" Width="120"/>
                    <DataGridTextColumn Header="House Number Ext" Binding="{Binding AddressHouseNumberExt}" Width="140"/>
                    <DataGridTextColumn Header="Postal Code" Binding="{Binding AddressPostal}" Width="110"/>
                    <DataGridTextColumn Header="Locality" Binding="{Binding AddressLocality}" Width="150"/>
                    <DataGridTextColumn Header="Country" Binding="{Binding AddressCountry}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Loading Overlay -->
            <Border Background="#CC000000"
                   Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ui:ProgressRing IsActive="{Binding IsBusy}"
                                Width="60"
                                Height="60"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
