<UserControl x:Class="HelloID.Vault.Management.Controls.ReferenceDataHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="1400">

    <Border Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
            Padding="16"
            BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
            BorderThickness="0,0,0,1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Title Section -->
            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          FontSize="20"
                          FontWeight="SemiBold"/>
                <TextBlock Text="{Binding Count, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          FontSize="14"
                          FontWeight="SemiBold"
                          Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                          Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Search Box -->
            <ui:AutoSuggestBox Grid.Column="1"
                               PlaceholderText="{Binding SearchPlaceholder, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               Text="{Binding SearchText, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                               QueryIcon="Find"
                               VerticalAlignment="Center"
                               MaxWidth="800"
                               HorizontalAlignment="Center"
                               Width="800"/>

            <!-- Action Buttons -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <ui:AppBarButton Icon="Refresh"
                                Label="Refresh"
                                Command="{Binding RefreshCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                <ui:AppBarButton Icon="Add"
                                Label="Add"
                                Command="{Binding AddCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                <ui:AppBarButton Icon="Undo"
                                Label="Reset Settings"
                                Command="{Binding ResetCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                ToolTip="Clear search and selection"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
